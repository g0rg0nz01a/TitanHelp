using System.ComponentModel.DataAnnotations;
// SWilson
// Create Enum RoleID drop down selection in TicketModel form.



namespace TitanHelp.Models
{
    public enum RoleStatus
    {
        Student = 0, 
        Faculty = 1,
        Staff = 2,
    }
}
